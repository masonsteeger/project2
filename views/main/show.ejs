<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <%-include('../partials/header.ejs')%>
    <h2><%=clip.title%></h2>
    <h3><%=clip.mode%></h3>
    <h5>Posted on: <%=clip.createdAt%></h5>
    <video width='640' height='480' controls>
      <source src="<%=clip.link%>" type="video/mp4">
    </video></br>
    <%if(currentUser){
      if(currentUser.username == clip.user){%>
    <a href="/edit/<%=clip.id%>"><button type="button">EDIT CLIP</button></a>
    <form class="" action="/clip/<%=clip.id%>?_method=DELETE" method="POST">
      <input type="submit" value="DELETE">
    </form>
  <%}
  }%>
    <a href="/home">This is a link Back Home</a>
  </body>
</html>
