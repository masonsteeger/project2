<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <%-include('../partials/head.ejs')%>
  </head>
  <body>
    <%-include('../partials/header.ejs')%>
    <div class="clip-show">
      <div class="stats">
        <h2><%=clip.title%></h2>
        <h2><%=clip.mode%></h2>
      </div>
      <video width='640' height='380' loop controls>
        <source src="<%=clip.link%>" type="video/mp4">
      </video></br>
      <h3>Clip Posted by: <a href="/user/<%=clip.user%>"><%=clip.user%></a></h3>
      <h5>Posted on: <%=clip.createdAt%></h5>
      <%if(currentUser){
        if(currentUser.username == clip.user){%>
      <a href="/edit/<%=clip.id%>"><button type="button">EDIT CLIP</button></a>
      <form class="" action="/clip/<%=clip.id%>?_method=DELETE" method="POST">
        <input type="submit" value="DELETE">
      </form>
    <%}
    }%>
    </div>
  </body>
</html>
